# T088 Red Run

Date: 2026-09-13. Node 24.19.0. Synthetic in-memory release records only.

```text
node --test scripts/canvas-reader/test/release-metadata.test.mjs
6 tests failed: release metadata and publication-gate validator is missing.
```

Required checks cover independent plugin versions, manifest/marketplace/README
agreement, unchanged unrelated versions, fork ownership/provenance/support,
explicit approvals, one provider per canvas, exact hashes/rollback targets,
non-substitutable native/live-workflow evidence, participant thresholds, two
bounded rollback rehearsals, and redacted failure reporting.

The positive specimen is explicitly synthetic and is not saved as an actual
release record. No owner, approval, study, native journey, or rollback result has
been fabricated for the current candidate. Missing real evidence must block the
publication gate even after the validator's unit tests pass.
