# T023: SDD Primary Review

Date: 2026-09-13. Windows, Node 24.19.0.

```powershell
node --test plugins/spec-kit-copilot-sdd/extensions/sdd-canvas/tests/artifact-review.test.mjs
```

Observed exit 1: five tests, five intended failures, no skips. Four identify the
missing SDD primary service; one identifies its absent adapter. Coverage includes
execution-unavailable reads, project constitution, feature/context isolation,
revision/deletion, capability preservation, GET-only mount, disposal, and focus.
