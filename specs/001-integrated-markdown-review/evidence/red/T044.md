# T044: One-Pass Heading Outline

Date: 2026-09-13. Windows, Node 24.19.0. Command from the reader package:

```powershell
node --import tsx --test test/markdown-outline.test.ts
```

Observed: four intended failures reporting the missing outline transform.
Cases cover ATX/Setext and rich plain-text labels, one rendering parse, duplicate
and pre-suffixed collisions, empty/punctuation fallback, code/comment exclusion,
Unicode, and independent reader namespaces.
