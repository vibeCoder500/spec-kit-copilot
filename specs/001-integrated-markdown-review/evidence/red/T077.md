# T077 Red Run

Date: 2026-09-13. Windows, Node 24.19.0. Owned temporary files only.

```text
node --test plugins/spec-kit-copilot-wizard/extensions/speckit-wizard-canvas/test/artifact-read.test.mjs
17 tests: 14 passed, 3 failed, 0 skipped.
```

Expected production gaps reproduced:

- Lexical validation accepted the encoded control filename `spec%0a.md` instead
  of denying it before filesystem access. The same corpus covers further encoded
  forms, Windows reserved aliases, invalid characters, devices, ADS, and metadata.
- An injected handle-close exception escaped as a raw error instead of a bounded
  `ArtifactReadError`. No private files or real credentials were involved.

One additional failure was in the injected special-file fixture: its target used
a Windows 8.3 temporary path while the reader used the canonical long path. The
fixture was corrected to compare canonical paths. Its focused rerun passed:

```text
node --test --test-name-pattern="non-regular targets" plugins/spec-kit-copilot-wizard/extensions/speckit-wizard-canvas/test/artifact-read.test.mjs
1 passed.
```

Actual Windows file symlinks, directory symlinks, and junction reparse points were
created successfully and denied. No privilege-related skip occurred. Special-file
categories and arbitrary canonical reparse escapes use injected metadata; this is
not evidence for every possible Windows reparse provider or a hostile same-user
filesystem race. Existing cap, encoding, replacement, and truncation tests passed.
