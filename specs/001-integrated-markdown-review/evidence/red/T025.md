# T025: SDD Primary Browser Journey

Date: 2026-09-13. Windows, Node 24.19.0, installed Edge channel, Playwright 1.63.0.

```powershell
npm --prefix plugins/spec-kit-copilot-wizard/extensions/speckit-wizard-canvas/ui/markdown-reader run test:browser -- sdd-shell.spec.mjs
```

Observed exit 1: one intended failure. The actual SDD shell and Specify preview
loaded, but its artifact view lacked a server-issued `[data-review-context]`.
The owned fixture was cleaned up. The journey also defines exact feature return,
focus, reader disposal, and zero workflow dispatch, mutation, and external calls.
