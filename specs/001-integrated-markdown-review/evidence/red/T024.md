# T024: Wizard Primary Browser Journey

Date: 2026-09-13. Windows, Node 24.19.0, installed Edge channel, Playwright 1.63.0.

```powershell
npm --prefix plugins/spec-kit-copilot-wizard/extensions/speckit-wizard-canvas/ui/markdown-reader run test:browser -- wizard-shell.spec.mjs
```

Observed exit 1: one intended failure. The existing shell and artifact trigger
loaded, but the opened overlay lacked `[data-review-context]` with a server-issued
`ctx_` identifier. This is the missing adapter behavior, not a host/setup failure.
The owned fixture was cleaned up. The journey also defines return focus, unchanged
workflow text, and zero dispatch, blocked writes, mutation, and external requests.
