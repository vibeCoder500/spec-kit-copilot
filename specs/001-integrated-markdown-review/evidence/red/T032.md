# T032: Wizard Related Artifacts

Date: 2026-09-13. Windows, Node 24.19.0.

```powershell
node --test --test-name-pattern="Wizard (discovery|resolves)" plugins/spec-kit-copilot-wizard/extensions/speckit-wizard-canvas/test/artifact-review.test.mjs
```

Observed exit 1: four intended failures. Discovery omitted research/custom/
checklist/contract/constitution files, pagination returned one primary instead
of 200 candidates, safe-link resolution was missing, and bounded scoped discovery
was absent. Temporary filesystem fixtures were cleaned up. Context-domain tests
already enforce cross-context cursors and cumulative counts including non-artifacts.
