# T079 Red Run

Date: 2026-09-13. Windows, Node 24.19.0, pinned jsdom 30.0.1. Synthetic content only.

From the reader build package:

```text
node --import tsx --test test/content-security.test.tsx
4 tests: 3 passed, 1 failed.
```

The native-link test expected a neutral anchor target controlled by the validated
host callback but observed `research.md#decision` directly in `href`. Browser
auxiliary/context-menu navigation can bypass an `onClick` callback, so raw local
and external targets must not provide an alternate navigation path around source
revision and host-policy validation. Footnotes remain trusted reader-scoped targets.

Active HTML/resource elements, remote/local/data images, unsafe schemes,
protocol-relative and credential-bearing URLs, and instruction-like prose passed.
No resource request or host command occurred. The test interceptor returns a
synthetic denial for any attempted resource request.

An initial fixture import used the retired `ResourceLoader` API. The pinned
package documentation identified jsdom 30's `requestInterceptor()` API; correcting
the fixture produced the meaningful red result above. That import failure is not
counted as a product security finding.
