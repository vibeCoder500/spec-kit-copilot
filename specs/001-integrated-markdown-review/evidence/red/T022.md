# T022: Wizard Primary Review

Date: 2026-09-13. Windows, Node 24.19.0.

```powershell
node --test plugins/spec-kit-copilot-wizard/extensions/speckit-wizard-canvas/test/artifact-review.test.mjs
```

Observed exit 1: six tests, six intended failures, no skips. Three identify the
missing `createWizardReviewService` export; three identify the absent browser
adapter. Coverage includes primary ownership, reads without execution, context
change, expected revision, GET-only mounting, disposal, reopen, and focus/scroll.
