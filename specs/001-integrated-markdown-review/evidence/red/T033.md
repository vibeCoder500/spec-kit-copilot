# T033: SDD Related Artifacts

Date: 2026-09-13. Windows, Node 24.19.0.

```powershell
node --test --test-name-pattern="SDD (discovery|resolves|packaged)" plugins/spec-kit-copilot-sdd/extensions/sdd-canvas/tests/artifact-review.test.mjs
```

Observed exit 1: four intended failures for absent scoped supporting documents,
pagination, safe reference resolution, and packaged bounded discovery. Tests also
specify expected-file denial, constitution inclusion, feature isolation, exact
revision checks, and counting non-Markdown entries toward the inspection limit.
Owned temporary files were removed after each case.
