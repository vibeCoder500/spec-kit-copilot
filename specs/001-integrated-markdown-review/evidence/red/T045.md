# T045: Safe Markdown Rendering

Date: 2026-09-13. Windows, Node 24.19.0. Command from the reader package:

```powershell
node --import tsx --test test/markdown-reader.test.tsx
```

Observed: four intended failures identifying the missing common reader component.
Cases cover Markdown/GFM, disabled task controls, table/code containment, raw-HTML
exclusion, non-loading images, supported link policy, and unique heading/footnote
IDs and references in two simultaneous readers.
