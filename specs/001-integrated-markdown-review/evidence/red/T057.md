# T057: Truthful Reader States

Date: 2026-09-13. Windows, Node 24.19.0. Command from the reader package:

```powershell
node --import tsx --test test/reader-states.test.tsx
```

Observed: 4 tests, 1 pass, 3 intended failures. Whitespace and headingless reads
were labeled `ready` instead of `empty`/`no-heading`; mount accepted a document
in `idle`. Changed/disconnected stale-content presentation already passed.
No external requests or source writes occurred.
