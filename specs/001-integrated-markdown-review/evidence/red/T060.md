# T060: Freshness Browser Journeys

Date: 2026-09-13. Windows, Node 24.19.0, Playwright 1.63.0, installed Edge channel.

```powershell
npm --prefix plugins/spec-kit-copilot-wizard/extensions/speckit-wizard-canvas/ui/markdown-reader run test:browser -- freshness.spec.mjs
```

Observed: four intended failures. Both canvases lacked Refresh artifact and the
disconnected state attribute. The mutation journeys use explicitly enabled owned
synthetic files and emit invalidations on the existing SSE stream. Stream-loss
tests close only fixture subscribers and temporarily reject reconnects. Cleanup
restores only hash-matching test mutations before removing the owned workspace.
No real repository file, workflow, or native App session was changed by these tests.
