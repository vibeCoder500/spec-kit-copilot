# T069: Trusted Clarification Rendering

Date: 2026-09-13. Windows, Node 24.19.0. Command from the reader package:

```powershell
node --import tsx --test test/clarifications.test.tsx
```

Observed: 3 tests, 1 pass, 2 intended failures for absent bound prose controls
and callback dispatch. Stale/foreign descriptors produced no controls. Tests keep
inline-code, comments, and unbound source markers inert and allow only descriptor
IDs to cross the trusted host callback boundary.
