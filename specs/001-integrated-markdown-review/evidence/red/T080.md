# T080 Red Run

Date: 2026-09-13. Windows, Node 24.19.0. Complete staged plugin copies in owned
temporary roots, with independently owned preview workspaces.

```text
node --test scripts/canvas-reader/test/package-security.test.mjs
6 tests: 2 passed, 4 failed, 0 skipped.
```

Both staged plugins passed the preview operation guards: zero child-process APIs
(including npm/Specify/Git/shell launch paths), zero remote fetches, zero write APIs
or write-mode opens, zero setup/write requests, and zero model dispatches. Source
hash comparisons stayed unchanged. Fixture creation/staging precedes the guard;
guard restoration precedes verified cleanup. No live workflow or install occurred.

Two expected adapter gaps reproduced in each packaged plugin:

- A synthetic server error's arbitrary `code` escaped unchanged instead of being
  restricted to the bounded review outcome set. Error stacks also need removal of
  module URL capabilities and local paths.
- A renderer-load exception escaped the fallback path as a raw rejected promise
  instead of presenting a bounded error state inside the existing preview.

All injected answer, capability, path, and OAuth-like strings were synthetic test
markers. No genuine private document, credential, or browser profile was accessed.
These HTTP/adapter tests complement, not replace, the actual-shell browser
zero-passive-request checks and later native App acceptance.
