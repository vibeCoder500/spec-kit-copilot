# T034: Wizard Navigation Journey

Date: 2026-09-13. Windows, Node 24.19.0, Playwright 1.63.0, installed Edge channel.

```powershell
npm --prefix plugins/spec-kit-copilot-wizard/extensions/speckit-wizard-canvas/ui/markdown-reader run test:browser -- wizard-shell.spec.mjs --grep "related artifacts"
```

Observed exit 1: the primary reader loaded, but the Artifacts selector did not
exist. The journey defines research selection, Back/Forward, a safe local link,
unchanged stage, focus return, zero dispatch/writes/mutation, and owned cleanup.
