# T087 Red Run

Date: 2026-09-13. Windows, Node 24.19.0. Owned temporary staging roots only.

```text
node --test --test-name-pattern="stages alone|independent payload" scripts/canvas-reader/test/sync-assets.test.mjs
6 tests failed, 0 skipped.
```

Every new case requested one existing plugin, but staging returned two. The
intended missing behavior is explicit single-plugin staging plus self-contained
payload/import verification. The cases cover deterministic inventories, missing
assets/notices, foreign/remote/absolute/uninstalled imports, forbidden private
files, and concurrent context isolation with no sibling plugin staged.

No installation, publication, remote, App, or workflow operation was performed.
Temporary staging roots were removed only by their owning fixture cleanup.
