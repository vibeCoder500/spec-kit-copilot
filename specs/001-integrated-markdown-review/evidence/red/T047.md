# T047: Responsive Reader Matrix

Date: 2026-09-13. Windows, Node 24.19.0, Playwright 1.63.0, installed Edge channel.

```powershell
npm --prefix plugins/spec-kit-copilot-wizard/extensions/speckit-wizard-canvas/ui/markdown-reader run test:browser -- reader-accessibility.spec.mjs --grep "at 360px"
```

Observed: both existing shells loaded their primary reader, then failed because
`data-reader-layout="compact"` was absent. These are intended missing responsive
reader failures. The full suite defines both canvases at 360/480/640/820/1024/1440
viewport widths, derives layout from actual container width, and covers keyboard
selection, destination focus, Escape, overflow, reduced motion, no passive requests,
synthetic screenshots, and a 200-percent CSS-zoom check. Other widths were not run
in this red slice; all remain required for green validation.
