# T078 Red Run

Date: 2026-09-13. Windows, Node 24.19.0. Actual existing HTTP servers with owned
synthetic workspaces; workflow and write endpoints blocked by the fixture.

```text
node --test --test-name-pattern="review HTTP|review responses|cursor generations cannot cross" plugins/spec-kit-copilot-wizard/extensions/speckit-wizard-canvas/test/server-integration.test.mjs plugins/spec-kit-copilot-sdd/extensions/sdd-canvas/tests/artifact-review.test.mjs
11 tests: 4 passed, 7 failed, 0 skipped.
```

Expected missing behavior reproduced:

- Wizard accepted a mismatched Host on an authenticated content request. Its
  existing token gate had no Host/Origin check; the new review surface needs one.
- Wizard static/denial responses omitted security headers; SDD unknown reader
  asset responses omitted the required cache/referrer/MIME headers.
- Both servers accepted duplicate capability query parameters.
- Both review body parsers accepted malformed UTF-8 after replacement decoding,
  returning 200 instead of typed `invalid_request`.

The corpus also requires denial of unknown/excessive query keys and incorrect
content type, bounded JSON, ordinary missing/wrong capabilities, and exact Origin.
SDD's existing capability/Host/Origin checks passed. Both servers rejected foreign
contexts and foreign/stale pagination cursors across concurrently active instances.
No workflow dispatched and no source body or real capability was included here.
