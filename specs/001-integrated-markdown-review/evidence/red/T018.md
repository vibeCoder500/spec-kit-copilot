# T018: Review Context Tests

Date: 2026-09-13. Windows, Node 24.19.0. T017's red record exists.

Command from the repository root:

```powershell
node --test plugins/spec-kit-copilot-wizard/extensions/speckit-wizard-canvas/test/artifact-review-context.test.mjs
```

Observed: exit 1; 8 tests, 0 pass, 8 fail, 0 skipped or cancelled. Every
failure reports `T018: the review-context domain is not implemented`.
The absent domain is the intended failure, not a syntax or dependency error.

Cases cover opaque/stable IDs, cross-context/instance rejection, current scope
and membership, expected outputs, cursor generation, 200-item pages, cumulative
10,000-entry inspection including non-artifacts, expiry, retained-state bounds,
path/input validation, and redacted errors. No workspace content is read.
Green evidence will be recorded after T020.
