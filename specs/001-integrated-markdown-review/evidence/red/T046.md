# T046: TOC Focus and Scroll Behavior

Date: 2026-09-13. Windows, Node 24.19.0. Command from the reader package:

```powershell
node --import tsx --test test/markdown-table-of-contents.test.tsx
```

Observed: four intended failures for absent scoped heading observation and TOC
components. Cases specify the 25-percent reading line, local lookup, scroll/resize/
animation-frame cleanup, focus trapping, Escape ordering, prior inert restoration,
destination focus, and cleanup when the container changes from compact to wide.
