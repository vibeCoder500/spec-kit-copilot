# T017: Bounded Artifact Read Tests

Date: 2026-09-13. Windows, Node 24.19.0. T016 passed before this task began.

Command from the repository root:

```powershell
node --test plugins/spec-kit-copilot-wizard/extensions/speckit-wizard-canvas/test/artifact-read.test.mjs
```

Observed: exit 1; 11 tests, 0 pass, 11 fail, 0 skipped or cancelled. Every
failure reports `T017: the bounded artifact-read primitive is not implemented`.
The absent module is the intended failure, not a syntax or dependency error.

Cases cover exact bytes/revisions, BOM, empty/whitespace, exact cap and overflow,
growth during bounded reads, invalid UTF-8/NUL, ordinary-file containment,
replacement/truncation, expected revision, handle cleanup, and redacted errors.
Synthetic temporary directories are created only after loading the primitive.
Green evidence will be recorded after T019.
