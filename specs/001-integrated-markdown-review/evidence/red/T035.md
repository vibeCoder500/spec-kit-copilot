# T035: SDD Navigation Journey

Date: 2026-09-13. Windows, Node 24.19.0, Playwright 1.63.0, installed Edge channel.

```powershell
npm --prefix plugins/spec-kit-copilot-wizard/extensions/speckit-wizard-canvas/ui/markdown-reader run test:browser -- sdd-shell.spec.mjs --grep "related artifacts"
```

Observed exit 1: the primary reader loaded, but the Artifacts selector was absent.
The journey defines related selection, Back/Forward, a local Markdown reference,
unchanged feature, focus return, zero dispatch/writes/mutation, and owned cleanup.
