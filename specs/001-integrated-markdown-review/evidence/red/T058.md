# T058: Wizard Freshness

Date: 2026-09-13. Windows, Node 24.19.0.

```powershell
node --test --test-name-pattern="Wizard (refresh|late)" plugins/spec-kit-copilot-wizard/extensions/speckit-wizard-canvas/test/artifact-review.test.mjs
```

Observed: 6 tests, 1 pass, 5 intended failures. Refresh and connection updates
were absent; deleting the primary invalidated the entire context instead of
retaining its expected descriptor. Late responses already could not replace a
newer selection or a closed reader. Atomic replacement revision checking passed
before the deletion assertion. Owned filesystem fixtures were cleaned up.
